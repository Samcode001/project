// import express from "express";
// const userRouter = express.Router();

// userRouter.get("/users", (req, res) => {
//   const { id } = req.body;
//   res.send({ id });
// });

// userRouter.post("/users", (req, res) => {
//   const { id, name, username, password } = req.body;
//   res.send({ id, name, username, password });
// });
